export default class BaseLayer{
   /**
    * set the parent clip
    */
   _setClip(clip){
     this.clip = clip
   }

   /**
    * set the parent video
    */
    _setVideo(video){
      this.video = video
    }

};
